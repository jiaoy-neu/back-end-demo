name: Manual Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: '发布版本号 (例如: 1.0.0)'
        required: true
      release_type:
        description: '发布类型'
        required: true
        default: 'stable'
        type: choice
        options:
          - stable
          - prerelease
          - draft
      changelog:
        description: '发布说明'
        required: false
        default: ''

jobs:
  build-frontend:
    runs-on: ubuntu-latest
    outputs:
      artifact_name: frontend-${{ github.run_id }}-${{ github.run_attempt }}
    
    steps:
    - name: 检出前端代码
      uses: actions/checkout@v4
      with:
        repository: jiaoy-neu/front-end-demo
        ref: master  # 根据你的分支修改
        path: frontend
    
    - name: 设置 Node.js 16.13.2
      uses: actions/setup-node@v4
      with:
        node-version: '16.13.2'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json
    
    - name: 安装依赖
      working-directory: ./frontend
      run: npm install
    
    - name: 构建前端
      working-directory: ./frontend
      run: npm run build
    
    - name: 上传前端构建产物
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.ARTIFACT_NAME }}
        path: frontend/dist/
        retention-days: 1
        
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: 检出后端代码
      uses: actions/checkout@v4

    - name: 下载前端构建产物
      uses: actions/download-artifact@v4
      with:
        name: frontend-${{ github.run_id }}-${{ github.run_attempt }}
        path: frontend-dist

    - name: 复制前端文件到后端目录
      run: |
        mkdir -p back/src/main/webapp/public
        cp -rf frontend-dist/* back/src/main/webapp/public/
        echo "前端文件已复制到后端目录:"
        ls -la back/src/main/webapp/public/
        
    - name: 设置 Java
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'oracle'  # 这是必需的参数
        
    - name: 创建 Git 标签
      run: |
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"
        git tag -a v${{ github.event.inputs.version }} -m "Release v${{ github.event.inputs.version }}"
        git push origin v${{ github.event.inputs.version }}
    - name: 构建 WAR
      run: |
        mvn clean install -Dmaven.test.skip=true
        cp target/*.war app-${{ github.event.inputs.version }}.war
    - name: 创建 Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ github.event.inputs.version }}
        name: Release v${{ github.event.inputs.version }}
        body: |
          ${{ github.event.inputs.changelog }}
          
          ## 🔧 使用说明
          运行应用:
          ```bash
          需要将WAR放到Tomcat中运行
          ```
          
          ## 📊 构建信息
          - 构建时间: $(date)
          - 提交: ${{ github.sha }}
          - 构建号: ${{ github.run_number }}
        draft: ${{ github.event.inputs.release_type == 'draft' }}
        prerelease: ${{ github.event.inputs.release_type == 'prerelease' }}
        files: |
          app-${{ github.event.inputs.version }}.war
        token: ${{ secrets.GITHUB_TOKEN }}
